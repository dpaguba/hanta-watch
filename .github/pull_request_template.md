## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `(cd etl && ruff check . && pytest -q)` passes
- [ ] `(cd web && npm run lint && npm run typecheck && npm test -- --run && npm run build)` passes
- [ ] Any new figure carries a citation to the document it came from
- [ ] Labels describe what is actually counted
